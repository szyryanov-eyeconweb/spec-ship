---
description: Phase 2 — Two-Agent TDD по TaskSpec, выдать BuildReport v1 + ADREntry
---

Используй скилл `build` для реализации TaskSpec через Two-Agent TDD.

Скилл находится в `.claude/skills/spec-ship/build/SKILL.md` — прочитай его и следуй инструкциям точно. Сохрани BuildReport v1 (и ADREntry при наличии) как JSON в `.ship/pipeline/{slug}/` строго по схеме.

$ARGUMENTS
