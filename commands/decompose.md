---
description: Phase 1 — разбить BusinessDoc на TaskSpec v1 (×N)
---

Используй скилл `decompose` для разбивки BusinessDoc на артефакты TaskSpec v1.

Скилл находится в `.claude/skills/spec-ship/decompose/SKILL.md` — прочитай его и следуй инструкциям точно. Сохрани каждый TaskSpec как JSON в `.ship/pipeline/{slug}/` строго по схеме.

$ARGUMENTS
