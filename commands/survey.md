---
description: Phase 0a — трассировка существующего кода от якоря, выдать Survey v1
---

Используй скилл `survey` для построения доказательной карты связанного кода от якоря.

Скилл находится в `.claude/skills/spec-ship/survey/SKILL.md` — прочитай его и следуй инструкциям точно. Сохрани Survey v1 JSON в `.ship/pipeline/{slug}/` (если BusinessDoc существует) или в `.ship/pipeline/_intake/` (до BusinessDoc) строго по схеме. Только наблюдение — ноль правок в src/tests/config.

$ARGUMENTS
