---
description: Phase 3 — ревью билда против TaskSpec и ADR, выдать ReviewReport v1
---

Используй скилл `review` для ревью BuildReport против TaskSpec и ADR.

Скилл находится в `.claude/skills/spec-ship/review/SKILL.md` — прочитай его и следуй инструкциям точно. Сохрани ReviewReport v1 как JSON в `.ship/pipeline/{slug}/` строго по схеме.

$ARGUMENTS
