---
description: Phase 0 — построить frozen BusinessDoc v1 из требований фичи
---

Используй скилл `shape-doc` для построения артефакта BusinessDoc v1.

Скилл находится в `.claude/skills/spec-ship/shape-doc/SKILL.md` — прочитай его и следуй инструкциям точно. Сохрани результат как JSON в `.ship/pipeline/{slug}/` строго по схеме.

$ARGUMENTS
